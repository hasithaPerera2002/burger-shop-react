
import '../../styles/loader.css'
const Loader = () => {
    return (
        <div className={"h-[100vh]  flex"}>
            <div className={'loader'}></div>
        </div>
    );
};

export default Loader;
